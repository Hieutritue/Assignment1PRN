<UserControl x:Class="Assignment1PRN.Views.RoomManageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Assignment1PRN.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Button Command="{Binding AddRoom}" Height="15" Width="45" HorizontalAlignment="Left" VerticalAlignment="Top" Margin=" 10 10 0 0" Content="Add customer" FontSize="6"></Button>
        <DataGrid ItemsSource="{Binding RoomList}">
            
        </DataGrid>
    </Grid>
</UserControl>
